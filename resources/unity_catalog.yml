resources:
  schemas:
    rag_schema:
      catalog_name: ${var.rag_catalog_name}
      name: ${var.rag_schema_name}
      comment: RAG starter schema for document ingestion

  volumes:
    rag_upload_volume:
      catalog_name: ${var.rag_catalog_name}
      schema_name: ${var.rag_schema_name}
      name: ${var.rag_volume_name}
      volume_type: MANAGED
      comment: Uploaded knowledge files for RAG ingestion
