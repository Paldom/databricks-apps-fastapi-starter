resources:
  secret_scopes:
    starter_scope:
      name: starter_scope_${var.resource_name_suffix}
      initial_manage_principal: users
      permissions:
        - user_name: admins
          level: WRITE
        - user_name: users
          level: READ
      secrets:
        LAKEBASE_PASSWORD: ""
        OPENAI_KEY: ""
